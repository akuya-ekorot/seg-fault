#include "main.h"

/**
 * average - returns the average of two floats
 * @num1: first float
 * @num2: second float
 * Return: the average of num1 and num2
*/
float average(float num1, float num2)
{
	float avg;

	avg = (num1 + num2) / 2;

	return (avg);
}
